org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -XX:+UseG1GC
org.gradle.parallel=false
org.gradle.daemon=false
org.gradle.configureondemand=false
android.useAndroidX=true
# The retained Phase 1 dependencies are already AndroidX-compatible. Disabling
# Jetifier avoids rewriting Flutter engine artifacts during every debug build.
android.enableJetifier=false
